import { Metadata } from "next"

const metadata :Metadata = ({
  "title" : "About Us",
  "description" : "This is about us page"

})
const page = () => {
  return (
    <div>page</div>
  )
}

export default page